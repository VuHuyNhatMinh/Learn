/* Task 1

function chooseName()
{
    para.innerHTML = names[Math.floor(Math.random() * names.length)];
}
*/

/*
Task 2
function draw()
{
    let x = Number(prompt("The x coordinate of the rectangle: ", ""));
    let y = Number(prompt("The x coordinate of the rectangle: ", ""));
    let w = Number(prompt("The width of the rectangle: ", ""));
    let h = Number(prompt("The height of the rectangle: ", ""));
    let c = prompt("The color of the rectangle: ", "");

    // Draw a square
    ctx.fillStyle(color);
    ctx.fillRect(x, y, w, h);
}
*/

/*
function random(a, b)
{
    return Math.floor(Math.random() * (b - a) + a);
}

function chooseName()
{
    return names[random(0, names.length)];
}

para.textContent = choseName();
*/
